# user data — contains password hashes & progress, NEVER commit
academy_data.json
academy_data.json.bak
academy_data.json.tmp
# scratch / test artifacts
_*.png
_*.html
*.log
.DS_Store
node_modules/
# scratch analysis artifacts (never part of the course)
phase_strip.png
geo_*.png
geo_*.svg
geo_*.js
*.mov
satellite_illumination*.gif
satellite_illumination*.mp4
*.mp4
# worksheet editor timestamped backups (regenerated on every save)
workbooks/**/*.bak
workbooks/**/*.tmp

# distributable bundles (built on demand — not repo content)
orbit-academy-*.zip

# ---- vendored third-party assets --------------------------------------------
# DELIBERATE: the repo ships our own schematic maps (always present, so the course
# is self-contained with zero network) but NOT redistributed third-party binaries.
# Each install runs `bash tools/fetch-vendor.sh` once on a networked machine, then
# copies the folder to the air-gapped target. See README "Requirements".
public/vendor/three.min.js
public/vendor/textures/earth_atmos_2048.jpg
public/vendor/textures/moon_1024.jpg
